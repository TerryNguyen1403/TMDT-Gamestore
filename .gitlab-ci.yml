stages:
    - build
    - push
    - deploy

build:
    stage: build
    variables:
        GIT_STRATEGY: clone
    script:
        - docker build -t $DOCKER_USERNAME/$PROJECT_NAME-server:$CI_COMMIT_TAG -f server/Dockerfile server
        - docker build -t $DOCKER_USERNAME/$PROJECT_NAME-client:$CI_COMMIT_TAG -f client/Dockerfile client
    tags:
        - gamestore-runner
    rules:
        - if: $CI_COMMIT_TAG